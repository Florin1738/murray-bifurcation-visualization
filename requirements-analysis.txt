# Optional dependencies for ML-based driver analysis
# Install with: pip install -r requirements-analysis.txt

# Machine learning
scikit-learn>=1.0.0,<2.0.0
pandas>=1.3.0,<3.0.0
matplotlib>=3.5.0,<4.0.0

# Deep learning (optional, for advanced analysis)
torch>=1.10.0,<3.0.0

# Advanced conditional feature importance
# Note: fippy must be installed separately:
#   pip install git+https://github.com/gcskoenig/fippy.git
# The analysis module will gracefully skip CFI plots if fippy is not available.
